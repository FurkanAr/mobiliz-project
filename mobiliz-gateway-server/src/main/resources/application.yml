server:
  port: 9090
spring:
  application:
    name: mobiliz-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka/

