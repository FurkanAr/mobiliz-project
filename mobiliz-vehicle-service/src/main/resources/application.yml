server:
  port: 9092

spring:
  application:
    name: mobiliz-vehicle-service

  jpa:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: create-drop
    show-sql: true

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: mypassword


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
